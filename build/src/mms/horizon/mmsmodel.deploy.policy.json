{
    "label": "deploy-policy-mms-model-$ARCH",
    "description": "A biz policy for mms model on $ARCH",
    "service": {
	"name": "$EDGE_OWNER.$EDGE_DEPLOY.mmsmodel",
	"org": "$HZN_ORG_ID",
	"arch": "$ARCH",
	"serviceVersions": [
	    {
		"version": "1.0.0",
		"priority":{}
	    }
	]
    },
    "properties": [
    ],
    "constraints": [
	"owner == $EDGE_OWNER",
	"deploy == $EDGE_DEPLOY",
	"mmsmodel == true"
    ],
    "userInput": [
    ]
}
