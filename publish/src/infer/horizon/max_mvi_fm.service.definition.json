{
    "org": "$HZN_ORG_ID",
    "label": "$SERVICE_NAME_MAX_MVI_FM-$ARCH",
    "description": "",
    "public": true,
    "documentation": "",
    "url": "$SERVICE_NAME_MAX_MVI_FM",
    "version": "$SERVICE_VERSION_MAX_MVI_FM",
    "arch": "$ARCH",
    "sharable": "singleton",
    "requiredServices": [
    ],
    "userInput": [
    ],
    "deployment": {
        "services": {
            "$SERVICE_NAME_MAX_MVI_FM": {
                "image": "${DOCKER_IMAGE_BASE_MAX_MVI}:$SERVICE_VERSION_MAX_MVI_FM",
                "privileged": true,
		"environment": [${MAX_MVI_ENVIRONMENT}],
		"binds": [
		    "${APP_BIND_HORIZON_DIR}/${MAX_MVI_FM_SERVICE_MODEL}:${MAX_MVI_FM_SERVICE_MODEL_PATH}:ro"
		],
		"ports": [
		    {
			"HostPort": "${MAX_MVI_FM_SERVICE_MODEL_HOST_PORT}:${MAX_MVI_SERVICE_MODEL_PORT}/tcp",
			"HostIP": "0.0.0.0"
		    }
		],
		"command": [${MAX_MVI_COMMAND}]
            }
        }
    }
}

